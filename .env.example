# Moltbook API key (from https://www.moltbook.com after registration)
MOLTBOOK_API_KEY=moltbook_sk_your_key_here

# Optional: path to Claude CLI binary (only needed if not using default PATH)
# CLAUDE_BIN=/usr/local/bin/claude
